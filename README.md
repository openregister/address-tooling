# AddressTooling.Umbrella

## To install

Install mongodb and Elixir if not already installed. On a Mac:

```
brew install elixir
brew install mongodb
```

Then load address data:

```
cd apps/address_tooling/
mix address.load
```
